/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { RadixPipe } from './radix.pipe';

describe('RadixPipe', () => {
  it('create an instance', () => {
    const pipe = new RadixPipe();
    expect(pipe).toBeTruthy();
  });
});
