/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { HyperbolicArcsinePipe } from './hyperbolic-arcsine.pipe';

describe('HyperbolicArcsinePipe', () => {
  it('create an instance', () => {
    const pipe = new HyperbolicArcsinePipe();
    expect(pipe).toBeTruthy();
  });
});
