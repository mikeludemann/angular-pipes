/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { CosinePipe } from './cosine.pipe';

describe('CosinePipe', () => {
  it('create an instance', () => {
    const pipe = new CosinePipe();
    expect(pipe).toBeTruthy();
  });
});
