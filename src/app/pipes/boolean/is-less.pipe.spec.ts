/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { IsLessPipe } from './is-less.pipe';

describe('IsLessPipe', () => {
  it('create an instance', () => {
    const pipe = new IsLessPipe();
    expect(pipe).toBeTruthy();
  });
});
