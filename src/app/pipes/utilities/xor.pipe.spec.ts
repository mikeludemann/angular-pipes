/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { XorPipe } from './xor.pipe';

describe('XorPipe', () => {
  it('create an instance', () => {
    const pipe = new XorPipe();
    expect(pipe).toBeTruthy();
  });
});
