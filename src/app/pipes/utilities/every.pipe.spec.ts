/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { EveryPipe } from './every.pipe';

describe('EveryPipe', () => {
  it('create an instance', () => {
    const pipe = new EveryPipe();
    expect(pipe).toBeTruthy();
  });
});
