/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { BeforePipe } from './before.pipe';

describe('BeforePipe', () => {
  it('create an instance', () => {
    const pipe = new BeforePipe();
    expect(pipe).toBeTruthy();
  });
});
