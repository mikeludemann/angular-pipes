/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { WherePipe } from './where.pipe';

describe('WherePipe', () => {
  it('create an instance', () => {
    const pipe = new WherePipe();
    expect(pipe).toBeTruthy();
  });
});
